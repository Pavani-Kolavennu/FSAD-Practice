{
  "private": true,
  "scripts": {
    "dev": "npm --prefix auth-frontend run dev",
    "build": "npm --prefix auth-frontend run build",
    "lint": "npm --prefix auth-frontend run lint",
    "preview": "npm --prefix auth-frontend run preview"
  },
  "dependencies": {
    "axios": "^1.13.6",
    "react-router-dom": "^7.13.2"
  }
}
